# Greetings
A simple Python program that generates dynamic greetings according to the current time of the system. The project demonstrates the use of the time module and conditional logic (if-elif-else). Inspired by tutorials from CodeWithHarry.
